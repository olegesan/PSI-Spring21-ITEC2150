package Session4.StartHere;

public class OnePLus6  extends Phone {

    /*
    Example of a default constructor in a child class.
    It doesn't call super method because it is called by default in every child class.
    If your parent class doesn't have a default method, you will have to call super([parameters]) with parameters
     */
    public OnePLus6() {

    }
}
